{
  "name": "browser-dynamics",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "wasm-pack build crates/sph --target web --out-dir pkg",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run && cd crates/sph && cargo test",
    "test:watch": "vitest",
    "clean": "rm -rf dist/ crates/sph/pkg/ crates/sph/target/",
    "cli": "cargo run --bin sph-cli"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "~5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "three": "^0.180.0"
  }
}
